//last element pivot off end of the array
//move any elements less than to left
// move any elements greater than to the right

let array = [3, 1, 2, 4, 5];

let quickSort = (array) => {
  let pivot = array.pop

	return array
	
	let lessThan = [];
	let greaterThan = [];
	
	for (let i = 0; array.length)
	if array[i] < pivot {
		push(lessThan)
	}
}

quickSort(array);
module.exports = quickSort;